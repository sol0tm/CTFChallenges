<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 50px;
        }

        #secret-page {
            display: none;
        }
    </style>
</head>
<body>

<?php
    // Simulate server-side processing for login
    if ($_SERVER["REQUEST_METHOD"] == "POST") {
        $username = $_POST["username"];
        $password = $_POST["password"];

        // Validate login credentials
        if ($username === "admin" && password_verify($password, '$2y$10$YourEncryptedPasswordHashHere')) {
            echo '<script>document.getElementById("login-form").style.display = "none";</script>';
            echo '<div id="secret-page">';
            echo '<h2>Welcome, Admin!</h2>';
            echo '<p>This is the secret content that should only be visible to authenticated users.</p>';
            echo '</div>';
        } else {
            echo '<p style="color: red;">Invalid username or password. Please try again.</p>';
        }
    }
?>

<div id="login-form">
    <h2>Admin Login</h2>
    <form method="post" action="<?php echo htmlspecialchars($_SERVER["PHP_SELF"]); ?>">
        <label for="username">Username:</label>
        <input type="text" name="username" required><br>

        <label for="password">Password:</label>
        <input type="password" name="password" required><br>

        <input type="submit" value="Login">
    </form>
</div>

</body>
</html>
